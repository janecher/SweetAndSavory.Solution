@{
  Layout = "_Layout";
}

@using SweetAndSavory.Models
@model SweetAndSavory.Models.Sweet

<span class="links">@Html.ActionLink("Home", "Index", "Home")</span>
<span class="links">@Html.ActionLink("Sweets list", "Index")</span>
<hr />
<h3>Add new sweet:</h3>
@using (Html.BeginForm())
{
  <div class="form-group">
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name, new {@class = "form-control", @required  = "required"})
  </div>
  @Html.Label("Select savories:")
  <br>
  @foreach (Savory savory in ViewBag.SavoryId)
  {
    @Html.Label(savory.Name)
    @Html.CheckBox("SavoryId", false, new { value = savory.SavoryId, @class="checkbox"})
  }
  <input type="submit" value="Add" class="btn" />
}
